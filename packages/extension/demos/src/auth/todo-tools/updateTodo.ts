import type { CheckedHostContext, InvokeOptions } from '../../../generated/tools/todo-tools.js';

export function checkUpdateTodoParameters(options: InvokeOptions, host: CheckedHostContext): InvokeOptions {
    void host;
    return options;
}

import type { CheckedHostContext, InvokeOptions } from '../../../generated/tools/todo-tools.js';

export function checkListTodosByCategoryParameters(options: InvokeOptions, host: CheckedHostContext): InvokeOptions {
    void host;
    return options;
}

# Test all api2ai MCP tools

Fuehre den Smoke-Test **aller** MCP-Tools **aller** Server aus `.cursor/mcp.json` aus.

1. Lies und befolge `.cursor/skills/api2ai-test-all-mcp/SKILL.md` vollstaendig.
2. Beginne die Ausfuehrung mit dem Praefix `api2ai /test:all` in deiner Antwort (Audit-Pflicht).
3. Schema-only: Parameter nur aus MCP-Tool-Descriptors, kein Repo-/OpenAPI-Wissen.

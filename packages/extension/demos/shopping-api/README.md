# shopping-api

Local JWT order API (port **3847**). OAuth via [`oauth-idp/`](./oauth-idp/) + MCP server **`shopping-api-oauth`**.

```bash
npm run demo:shopping-api
node shopping-api/get-token.mjs alice   # dev JWT only (no /login route)
```

#Col3:23

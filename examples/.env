TMDB_ACCESS_TOKEN=set-in-env-local

# bearerSealed (GitHub MCP): path is safe to commit; PEM stays gitignored. Relative paths are
# resolved from process.cwd() and parent dirs (so MCP hosts with a non-root cwd still find the file).
API2AI_SEAL_PRIVATE_KEY=examples/seal-keys/private.pem
